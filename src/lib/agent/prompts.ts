export const SYSTEM = `You are a careful newsletter research editor. Use only supplied source evidence. Never invent facts, dates, quotes, publishers, or URLs. Write concise, practical copy for technology professionals.`;
export const planPrompt = (goal:string) => `${SYSTEM}\nCreate a structured research plan for this goal: ${goal}. Search for recent news about AI agents. Use 2-3 diverse concise queries, choose 5-7 articles, and make a useful editorial angle.`;
export const selectionPrompt = (sources:string) => `${SYSTEM}\nSelect up to 9 strongest candidates from these search results. Prioritize AI-agent relevance, recency, source diversity and substantive snippets. Return an array of URLs only.\n${sources}`;
export const summaryPrompt = (articles:string) => `${SYSTEM}\nCreate grounded summaries for every usable article below. A snippet-only article may be used only when its snippet clearly supports the summary; preserve evidenceType.\n${articles}`;
export const draftPrompt = (summaries:string) => `${SYSTEM}\nDraft a clean weekly newsletter from exactly 5-7 supplied summaries. Preserve their titles, URLs, source, date, evidenceType and claims.\n${summaries}`;
export const critiquePrompt = (draft:string) => `${SYSTEM}\nCritique this newsletter. Check grounded links, 5-7 items, duplication, recency, scanability and unsupported claims. Score 0-100. Choose revise below 85 or if any grounding/count issue exists.\n${draft}`;
export const revisionPrompt = (draft:string, critique:string) => `${SYSTEM}\nRevise this newsletter exactly once using the critique. Keep only grounded supplied items and 5-7 items.\nDRAFT:${draft}\nCRITIQUE:${critique}`;
